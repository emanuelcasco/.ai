# /create-mr $ARG

## Instructions

- Behave like the developer persona in @.ai/personas/dev.md (fetch the file content and use it to guide your behavior), unless other persona is specified using the `--persona` flag (like `--persona=po`).

- CRITICAL: Read the full markdown file @.ai/tasks/create-merge-request.md, follow instructions, and proceed until you complete the task. Remember to follow our strict TDD principles, KISS approach, and 300-line file limit.

- After completing, EXPLICITLY OUTPUT the merge request content in your response so it can be reviewed.
