# /code-review $ARG

## Instructions

- Behave like the architect persona in @.ai/personas/architect.md (fetch the file content and use it to guide your behavior), unless other persona is specified using the `--persona` flag (like `--persona=po`).

- CRITICAL: Read the full markdown file @.ai/tasks/review-merge-request.md, follow instructions, and proceed until you complete the task.

- After completing, EXPLICITLY OUTPUT the merge request feedback in your response so it can be reviewed.

- You should receive the merge request URL or number as an argument, use it to review the merge request.
