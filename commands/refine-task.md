# /refine-task $ARG

## Instructions

- Behave like the product owner persona in @.ai/personas/po.md (fetch the file content and use it to guide your behavior), unless other persona is specified using the `--persona` flag (like `--persona=architect`).

- CRITICAL: Read the full markdown file @.ai/tasks/refine-story-task.md, follow instructions, and proceed until you complete the task. Remember to follow our strict TDD principles, KISS approach, and 300-line file limit.

- After completing, EXPLICITLY OUTPUT the refined task in your response so it can be reviewed.
